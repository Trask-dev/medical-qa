[pytest]
markers =
    integration: 真实LLM/数据库集成测试 (需API密钥, 通过 -m integration 显式运行)
    slow: 运行时间 >10s 的测试
    timeout: 测试超时标记 (需 pytest-timeout 插件)

testpaths =
    tests/

addopts =
    -v
    --strict-markers
    -m "not integration"
